<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">

        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>


    <!-- radiocustomized action bar theme -->
    <style name="DisplaySchedulesActionBarTheme" parent="@android:style/Theme.Holo.Light">
       <!-- <item name="android:logo">@drawable/ic_radio_actionbar_icon</item>-->
        <item name="homeAsUpIndicator">@drawable/ic_back</item>
        <item name="android:homeAsUpIndicator">@drawable/ic_back</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>

    </style>

    <style name="DisplaySchedulesActionBarThemeAppCompat" parent="Theme.AppCompat">
        <!-- <item name="android:logo">@drawable/ic_radio_actionbar_icon</item>-->
        <item name="homeAsUpIndicator">@drawable/ic_back</item>
        <item name="android:homeAsUpIndicator">@drawable/ic_back</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>

    </style>

    <style name="Theme.AppCompat.Light.NoActionBar.FullScreen" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@android:color/white</item>
    </style>

    <style name="Dialog_No_Border">
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="MaterialCircularProgress" parent="Theme.AppCompat.Light">
        <item name="colorAccent">@color/material_circular_loading_color</item>
    </style>

    <style name="LinearProgress" parent="Theme.AppCompat.Light">
        <item name="colorAccent">@color/material_circular_loading_color</item>

    </style>

    <style name="SMAccessSettingsTheme" parent="@style/Theme.AppCompat.Light">
        <item name="colorPrimary">@color/action_bar_color</item>
        <item name="android:textColorPrimary">@color/action_bar_text_color</item>
    </style>

    <style name="WebView_AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">

        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/green</item>
        <item name="colorPrimaryDark">@color/green</item>
        <item name="colorAccent">@color/green</item>

    </style>
</resources>
